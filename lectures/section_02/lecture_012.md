# Lecture 12: Installing Libraries with pip and UV

## Managing Dependencies
Always pin dependency versions in a `requirements.txt` file for repeatable builds across development and production environments.