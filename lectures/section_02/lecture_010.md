# Lecture 10: Create Virtual Environments with UV

## What is UV?
**UV** is an extremely fast Python package manager written in Rust, built by Astral (creators of Ruff). It runs **10x-100x faster** than standard `pip`.