# Lecture 27: Encapsulation

## Key Concepts
- **Private & Protected Attributes**: Prefixing attributes with `_` and `__` for access control.
- **Properties (`@property`)**: Managing attribute access securely using getters and setters.