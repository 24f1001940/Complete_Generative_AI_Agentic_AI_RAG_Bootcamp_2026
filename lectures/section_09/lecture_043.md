# Lecture 43: Data manipulation with Pandas

## Key Concepts
- **`groupby` & Aggregations**: Segmenting datasets and calculating dynamic summary statistics (`mean`, `sum`, `count`).
- **Data Merging**: Joining disparate datasets on common key columns using `pd.merge()`.
- **Relational Operations**: Performing database-style join and aggregation operations in memory.