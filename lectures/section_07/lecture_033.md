# Lecture 33: Generators

## Key Concepts
- **Generator Functions**: Defining functions with `yield` instead of `return`.
- **Memory Efficiency**: Computing items on-demand rather than holding complete collections in memory.
- **Generator Expressions**: Using compact syntax `(expr for item in iterable)` for lazy iteration.