# Lecture 20: Reading and Writing Text Files

## Key Constructs
- **Modes**: Opening files in write (`w`), append (`a`), and read (`r`) modes.
- **Context Managers**: Utilizing `with` blocks to ensure safe automatic file closing.
- **Line Processing**: Reading and cleaning line collections using `.readlines()` and string stripping.