# Lecture 21: Reading Structured Data Files

## Key Constructs
- **JSON Serialization**: Parsing and writing structured JSON data with `json.load()` and `json.dump()`.
- **CSV Processing**: Reading tabular files as dictionaries using `csv.DictReader`.